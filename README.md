# Realsense-d435i

To Set Up:
Open Command Prompt
Download these packages by running these commands:
pip install opencv-python
pip install realsense2

If pip is not recognised as an internal or external command:
Download Python
Run in command prompt: python -m pip